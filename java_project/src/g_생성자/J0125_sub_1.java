package g_생성자;

public class J0125_sub_1 {
     String name;
     int kor;
     double ave;
     
     
	 String getName() {
		return name;
	}
	 void setName(String name) {
		this.name = name;
	}
	 int getKor() {
		return kor;
	}
	 void setKor(int kor) {
		this.kor = kor;
	}
	 double getAve() {
		return ave;
	}
	 void setAve(double ave) {
		this.ave = ave;
	}	 
     
     //전역변수는 지역변수에서 사용할 수 있음, 지역변수를 통해 전역변수의 값을 지정해 주고(set) 그 값을 다시 사용할 수 있음(get).
     
    
	
	
}
